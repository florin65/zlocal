depends zlib
depends %OSSL
depends coreutils
depends psmisc

optional_depends Linux-PAM \
                 "--with-pam" \
                 "--without-pam" \
                 "for Linux PAM support"

optional_depends libedit \
                 "--with-libedit" "" \
                 "generic line editing support"

optional_depends ldns \
                 "--with-ldns" "" \
                 "for DNSSEC support" \
                 "n"

optional_depends heimdal \
                 "--with-kerberos5=/usr" "" \
                 "for Heimdal Kerberos5/GSSAPI support" \
                 "n"

optional_depends libfido2 \
                 "--with-security-key-builtin" "" \
                 "for FIDO/U2F devices support over USB or NFC" \
                 "n"

optional_depends xauth \
                 "--with-xauth=/usr/bin/xauth" "" \
                 "X11 forwarding support" \
                 "n"

optional_depends x11-ssh-askpass "" "" "for passphrase input in X11" "n"
